While_Invalid_11.whiley:6: loop invariant may not be restored {i=0, i$1=0, i$2=1, ls=[-1], sum=0, sum$1=0, sum$2=-1}
    while i < |ls| where (i >= 0) && (sum >= 0):
                         ^^^^^^^^^^^^^^^^^^^^^^
