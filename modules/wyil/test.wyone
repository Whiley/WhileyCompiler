term True
term False
class Bool as True | False

term Var(string)

// ==================================================
// Theory of Logical Connectives
// ==================================================

// --- Logical Not ---
term Not(Term)

rewrite Not(Bool b):
    => True(), if b is False
    => False()

rewrite Not(Not(* x)):
    => x

// rewrite Not(And(Term... xs)):

// rewrite Not(Or(Term... xs)):

class Term as Bool | Var | Not
