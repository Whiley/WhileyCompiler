term True
term False
class Bool as True | False

term Var(string)
term And{Term...}

//rewrite And{Term x, Term y, Term... xs}:
//    => And (x ++ xs), if x == y

rewrite And{And{Term... xs}, Term... ys}:
    => And (xs ++ ys)

class Term as Bool | Var






