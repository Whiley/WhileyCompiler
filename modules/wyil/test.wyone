term True
term False
class Bool as True | False

term Var(string)
term Not(Term)
term Or{Term...}
term Equal[Term,Term]

rewrite Or{Term x, Term y, Term... ys}:
    => Or(x ++ ys), if x == y

class Term as Bool | Var | Not | Or