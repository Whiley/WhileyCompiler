term True
term False
class Bool as True | False
term Not(Term)
term And[Term]
term Var(String)

rewrite Not(True x):
    => False

rewrite Not(False x):
    => True

rewrite Not(Not(* x)):
    => x

rewrite And[Term t, Term ts]:
    => t

class Term as Bool | Not | Var