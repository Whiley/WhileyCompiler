// An implementation of the Lambda Calculus :P

term Var(string)
term Num(int)
term List[Expr...]
term IndexOf[Expr, int]
class Expr as Num | Var | List

reduce IndexOf[List[Expr... es],int idx]:
    => es[idx], if idx >= 0 && idx < |es|

