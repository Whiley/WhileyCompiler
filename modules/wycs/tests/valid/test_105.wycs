function Union({int} lhs, {int} rhs) => ({int} r) where: 
  forall(int e ; e in lhs ==> e in r)
  forall(int e ; e in rhs ==> e in r)
  forall(int e ; e in r ==> (e in lhs || e in lhs))

assert: 
  forall(int x, {int} xs, {int} ys): 
    if:
      x in xs
    then: 
      x in Union(xs,ys)
