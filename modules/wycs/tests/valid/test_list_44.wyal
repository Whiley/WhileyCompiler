import wycs.core.Array

define invariant([int] items, int n) is:
    forall (int i):
        if:
            i < n
        then:
            items[i] == 0
            
assert "loop invariant not restored":
    forall ([int] items, [int] nitems, int n):
        if:
            invariant(items,n)
        then:
            Array.update(items,nitems,n,0)
            invariant(nitems,n+1)


